export class Theme {
  constructor(public id: number,
              public name: string,
              public description: string,
              public tags: Array<string>,
              public image: string) {
  }
}
